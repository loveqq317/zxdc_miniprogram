<view class="tab-controll">
  <block wx:for="{{titles}}" wx:key="{{index}}">
    <view class="tab-item {{currentIndex == index ? 'active':''}}" bind:tap="handleItemClick"
    data-index="{{index}}">
     <text>{{item}}</text>
    </view>
   
  </block>
</view>
